export default class Validation {
    static isUrl(url: string): boolean;
    static isValidNumberOfConnections(numOfConnections: number): boolean;
    static isDirectory(directory: string): boolean;
}
